# Narrative Game System

## Setup

1.  **Install dependencies**:
    ```bash
    uv sync
    ```

2.  **Environment**:
    Create a `.env` file with your API key:
    ```
    GOOGLE_API_KEY=your_key_here
    ```

## Run

```bash
uv run examples/rickshaw_accident/run_example.py
```
